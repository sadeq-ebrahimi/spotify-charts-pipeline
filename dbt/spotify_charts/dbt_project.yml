name: 'spotify_charts'
version: '1.0.0'
config-version: 2

profile: 'spotify_charts'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  spotify_charts:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +as_columnstore: false